/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "fakestoreapi.com",
      "fakeapistore.com",
      "dummyjson.com",
      "hadigems.com",
    ],
  },
};

module.exports = nextConfig;
